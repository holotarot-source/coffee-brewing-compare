import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Coffee Brewing Method Comparator | Science-Backed Analysis",
  description:
    "Compare coffee brewing methods side-by-side with peer-reviewed research. Understand extraction chemistry, health effects, and flavor profiles of French Press, Pour Over, Espresso, AeroPress, Cold Brew, Moka Pot, Turkish, and Siphon brewing.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
